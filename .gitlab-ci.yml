image: python

before_script:
  - lsb_release -a
  - python --version
  - which python
  - pip install -r requirements.txt

stages:
  - Static Analysis

pylint:
  stage: Static Analysis
  script:
  - pylint mxmc
